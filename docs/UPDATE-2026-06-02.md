# Update 2026-06-02 - Local-only documentation cleanup

## Summary

This update aligns project documentation with the current local-only `diepxuan/9router` workflow.

## Changes

- Keep only English and Vietnamese documentation.
- Remove Chinese, Japanese, and Spanish GitBook content.
- Remove non-Vietnamese localized root README files.
- Normalize local service references to port `20128`.
- Normalize web fetch documentation to `/v1/web/fetch`.
- Replace external Docker image guidance with local image build/run commands.
- Clarify that upstream `decolua/9router` is read-only reference for this workspace.
- Add local-fork boundary guidance for fork-only code under `src/diepxuan/**` and `open-sse/diepxuan/**`.
- Strengthen local/non-local exposure security notes for dashboard password, API key enforcement, and SQLite data protection.

## Validation

- `git diff --check -- '*.md'` passed.
- Stale reference scan passed for retired local port references, old web-fetch endpoint references, non-English/Vietnamese GitBook paths, removed localized README files, external published Docker image references, and raw upstream skill links.
