# Checklist trước khi Push

> **QUAN TRỌNG:** Đọc trước khi thực hiện `git push`

---

## Câu hỏi bắt buộc

- [ ] Commit message đã đầy đủ chưa?
- [ ] Có file nào chưa add không?
- [ ] Có muốn amend commit không? (Nếu CÓ → DỪNG → Tạo commit mới)
- [ ] Đã kiểm tra nội dung thay đổi chưa?

---

## Lưu ý quan trọng

### TUYỆT ĐỐI KHÔNG
- `git commit --amend` sau khi đã push
- Force push mà không hỏi Sếp

### THAY VÌ AMEND
| Muốn | Thay vì | Làm |
|------|---------|-----|
| Sửa message | `git commit --amend` | `git commit -m "message moi"` |
| Thêm file | `git commit --amend --no-edit` | `git add` + `git commit -m "..."` |
| Gộp commits | `git rebase -i` | Tạo PR mới |

### KHI NÀO FORCE PUSH ĐƯỢC PHÉP
- Sếp cho phép
- Nhánh chưa merge
- Không ai khác đang làm việc trên nhánh đó

---

## Quy trình đúng

```
1. git status          → Kiểm tra thay đổi
2. git diff --cached   → Xem nội dung sẽ commit
3. git commit -m "..." → Commit đầy đủ (không amend)
4. git push            → Push bình thường
```

---

*Nếu không chắc chắn → hỏi Sếp trước.*